# bluebook1

after fork & clone, create a file in the repo with you first initial-lastname.

do a add, commit, push. DM the repo's URL to Paul and Kris, then you can start solving the problems.
After each problem you solve, doa commit and push.
